worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    # Upstream for Go Backend
    upstream backend {
        server 127.0.0.1:8080;
    }

    server {
        listen       443 ssl;
        server_name  localhost;

        # SSL Configuration
        ssl_certificate      /home/toxicsoda/campus-logistics/deployment/certs/server.crt;
        ssl_certificate_key  /home/toxicsoda/campus-logistics/deployment/certs/server.key;

        ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;
        ssl_ciphers  HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers  on;

        # Frontend Static Files
        location / {
            root   /home/toxicsoda/campus-logistics/frontend/dist;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        # Backend API Proxy
        location /api/ {
            proxy_pass http://backend/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
